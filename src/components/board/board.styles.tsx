import styled from 'styled-components';

export const BoardContainer = styled.div`
  display: flex;
  gap: 1rem;
  padding: 1rem;
  width: 100vw;
  overflow-x: auto;
  overflow-y: hidden;
  max-height: calc(100vh - 10%);
`;
